pre-push:
  parallel: true
  commands:    
    rspec:    
      run: bundle exec rspec 
      fail_text: "RSpec tests failed"
    rubocop:
      run: bundle exec rubocop
      fail_text: "RuboCop violations found"    
    steep:
      run: bundle exec steep check
      fail_text: "Steep type checking failed"
pre-commit:
  parallel: true
  commands:    
    rspec:    
      run: bundle exec rspec 
      fail_text: "RSpec tests failed"    